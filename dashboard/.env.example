# Clocksy backend API
NEXT_PUBLIC_BACKEND_URL=http://localhost:3001

# Shared JWT secret (server-only) — must match the backend's JWT_SECRET so the
# dashboard can verify session tokens in middleware.
JWT_SECRET=change-me-to-a-long-random-secret
